import { ArrowRightToLine, PlusCircleIcon, SignInIcon, SignOutIcon, TickIcon, XIcon } from '@components/icons'
import React from 'react'

const Test = () => {
  return (
    <div className="flex flex-col space-y-4">
      <ArrowRightToLine width={50}/>
      <SignInIcon width={50}/>
      <SignOutIcon width={50}/>
      <PlusCircleIcon width={50}/>
      <TickIcon width={50}/>
      <XIcon width={50}/>
      
    </div>
  )
}

export default Test